pub enum DamageType {
    BLEED,
    COLD,
    DISEASE,
    FIRE,
    MAGIC,
    OBLIVION,
    PHYSICAL,
    POISON,
    SHOCK,
}

pub enum DamageSize {
    SINGLETARGET,
    MULTITARGET,
}

pub enum DamageTime {
    INSTANT,
    DOT,
}
